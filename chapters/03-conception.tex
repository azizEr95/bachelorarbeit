\chapter{Konzeption}
Die in Kapitel~2 erarbeiteten funktionalen und nicht-funktionalen Anforderungen sowie die definierten Use Cases bilden die Grundlage für die Konzeption der mobilen Anwendung. In diesem Kapitel wird zunächst die Softwarearchitektur sowie die bestehende Backend-Infrastruktur als Ausgangspunkt beschrieben. Darauf aufbauend werden das Komponentendiagramm, das Navigationskonzept, die Mockups der Benutzeroberfläche sowie abschließend ein Aktivitätsdiagramm zum Terminbuchungsprozess vorgestellt.

\section{Softwarearchitektur}

Selbst innerhalb der Informatik existiert keine einheitlich anerkannte Definition des Begriffs Softwarearchitektur~\cite[S.~1]{richards2021}. Dennoch lässt sich der Begriff inhaltlich eingrenzen. Softwarearchitektur umfasst die zentralen Grundsätze und Vorgaben, nach denen ein System gegliedert und in Komponenten sowie Schnittstellen aufgeteilt wird. Sie beschreibt zudem, wie diese Elemente untereinander und mit ihrer Umgebung
interagieren. Auf dieser Basis werden verbindliche Leitlinien für alle Phasen des Systemlebens definiert, von der Anforderungsanalyse über den Entwurf und die Implementierung bis hin zum Betrieb und zur Pflege des Systems, ebenso wie für die zugehörigen Entwicklungs- und Betriebsstrukturen~\cite[S.~21]{gharbi2020}. Der Schwerpunkt dieses Kapitels liegt auf dem Entwurf der Software.

\subsection{Architekturübersicht}
\label{sec:architektur}

Die Architektur von \textit{petappoint} basiert auf einer Schichtenarchitektur. Bei einer Schichtenarchitektur werden Komponenten in hierarchischen Ebenen angeordnet, wobei eine Komponente einer höheren Schicht Anfragen an eine Komponente einer niedrigeren Schicht stellen und auf deren Antwort warten kann~\cite[S.~57]{tanenbaum2007}. Innerhalb dieser Struktur herrscht das Client-Server-Prinzip. Der Server, der bestimmte Dienste bereitstellt, und der Client bzw. mehrere Clients, die diese Dienste durch das Senden von Anfragen in Anspruch nehmen und anschließend auf die Antwort des Servers warten~\cite[S.~79]{tanenbaum2007}. Die Kommunikation zwischen Client und Server erfolgt über eine REST-API (Representational State Transfer). RESTful-Architekturen zeichnen sich dadurch aus, dass Ressourcen über ein einheitliches 
Benennungsschema identifiziert werden, alle Dienste dieselbe Schnittstelle mit den vier Operationen POST, PUT, GET und DELETE (Erstellen, Aktualisieren, Abrufen, Löschen) anbieten, Nachrichten vollständig selbstbeschreibend sind und Dienste nach der Ausführung einer Operation keinerlei Informationen über den Aufrufer speichern~\cite[S.~66]{tanenbaum2007}.

Die Schichtenarchitektur gliedert sich in drei Schichten: 
\begin{itemize}
    \item \textbf{Präsentationsschicht (Frontend):} Zuständig für die Darstellung 
    der Benutzeroberfläche und repräsentiert den Client des Systems.
    \item \textbf{Anwendungsschicht (Backend):} Verarbeitet eingehende 
    HTTP-Anfragen, validiert Eingabedaten und steuert den Datenbankzugriff. 
    Diese Schicht übernimmt die Rolle des Servers.
    \item \textbf{Datenschicht (Datenbank):} Verantwortlich für die Speicherung 
    und Verwaltung aller persistenten Daten der Anwendung.
\end{itemize}

Die mobile Anwendung wird in der Präsentationsschicht ergänzt und ist somit ausschließlich für die Darstellung und Nutzerinteraktion zuständig. Da das 
Backend von \textit{petappoint} im Rahmen eines vorangegangenen Projektmoduls bereits entwickelt wurde, beschränkt sich der Fokus auf die Konzeption und Umsetzung der Präsentationsschicht. Abbildung~\ref{fig:softwarearchitecture} veranschaulicht den beschriebenen Aufbau grafisch.

\begin{figure}[H]
    \centering
    \includesvg[width=1\linewidth]{figures/software-architecture.svg}
    \caption{Schichtenarchitektur von \textit{petappoint}}
    \label{fig:softwarearchitecture}
\end{figure}

\subsection{Bestehende Backend-Architektur als Grundlage}

Das Backend von \textit{petappoint} wurde im Rahmen eines vorangegangenen Projektmoduls entwickelt und bildet die technische Grundlage dieser Arbeit. Es stellt sämtliche Geschäftslogik und Datenhaltung bereit, auf die die mobile Anwendung über die REST-API zugreift.

Die zugrunde liegende Datenbankstruktur ist in Abbildung~\ref{fig:erdiagramm} als ER-Diagramm dargestellt. Ein Entity-Relationship-Diagramm (ER-Diagramm) ist eine grafische Methode zur Darstellung der logischen Struktur einer Datenbank. Das Modell basiert auf drei Grundkonzepten: Entitäten repräsentieren reale Objekte oder Konzepte, 
Attribute beschreiben deren Eigenschaften, und Beziehungen stellen die Verbindungen zwischen den Entitäten dar~\cite[S.~29]{watt2014}. Die Kardinalitäten geben dabei an, wie viele Instanzen einer Entität mit wie vielen Instanzen einer anderen Entität in Beziehung stehen können und werden in der Krähenfuß-Notation durch spezielle Symbole dargestellt, wie in Abbildung~\ref{fig:erdiagramm} als Legende gezeigt. 

Das Schema umfasst die zentralen Entitäten \textit{persons}, \textit{animals} und \textit{veterinarypractices}, die den Kern der Anwendungslogik bilden. Tierhalter werden als \textit{persons} erfasst und können keine oder mehrere Tiere (\textit{animals}) anlegen, aber ein Tier kann nicht ohne eine Person existieren, sodass eine $1{:}n$-Beziehung zwischen \textit{persons} und \textit{animals} besteht. Jedes Tier ist dabei genau einem Tiertyp (\textit{animal\_types}) zugeordnet und ein Tiertyp kann vielen Tieren zugeordnet werden. Ein Tiertyp kann wiederum mehrere Rassen (\textit{animal\_races}) umfassen, wobei jede Rasse genau einem Tiertyp zugeordnet ist, was einer $1{:}n$-Beziehung zwischen \textit{animal\_types} und \textit{animal\_races} entspricht.

Tierarztpraxen werden über \textit{veterinarypractices} abgebildet und stehen in einer $1{:}n$-Beziehung zu den zugehörigen Tierärzten 
(\textit{veterinarians}), da eine Praxis mehrere Tierärzte beschäftigen kann, aber ein Tierarzt genau einer Tierarztpraxis zugeordnet werden kann. Termine werden in der Tabelle \textit{appointments} gespeichert und können optional einem Tier, einem Tierarzt sowie einer gebuchten Leistung (\textit{services}) zugeordnet werden, was einer 
$0{:}n$-Beziehung zu diesen Entitäten entspricht. Ein Termin kann allerdings nicht ohne Tierarztpraxis existieren, aber eine Praxis kann mehrere Termine besitzen, was einer $1{:}n$-Beziehung entspricht.

Darüber hinaus enthält das Schema eine Favoritenfunktion, die über die 
Relationstabelle \textit{person\_has\_favorized\_veterinarypractice} abgebildet wird. Da ein Tierhalter mehrere Praxen als Favoriten speichern kann und eine Praxis gleichzeitig von mehreren Tierhaltern favorisiert werden kann, liegt hier eine $n{:}m$-Beziehung vor.

\begin{figure}[H]
    \centering
    \includesvg[inkscapelatex=false, width=1.0\linewidth]{figures/er-diagram.svg}
    \caption{ER-Diagramm der Datenbankstruktur von \textit{petappoint}}
    \label{fig:erdiagramm}
\end{figure}

\subsection{Kritische Betrachtung der Backend-Infrastruktur}
Das bestehende Backend bildet die technische Grundlage für die Entwicklung der mobilen Anwendung, weist jedoch Schwachstellen auf, die im Rahmen einer Weiterentwicklung berücksichtigt werden sollten.

Ein erster Kritikpunkt betrifft die Rollentrennung zwischen 
\textit{veterinarians} und \textit{persons}. Da Tierärzte über einen Fremdschlüssel auf \textit{persons} verweisen, teilen beide Entitäten dieselbe Tabelle, was die Abgrenzung der Rollen im System erschwert und zu Inkonsistenzen führen kann. Eine explizite Trennung der Nutzerrollen würde die Wartbarkeit und Erweiterbarkeit des Systems verbessern.

Eng damit verbunden ist das Fehlen einer Rollenhierarchie innerhalb einer Tierarztpraxis. Im aktuellen Schema verfügen alle Tierärzte einer Praxis über dieselben Zugriffsrechte, ohne dass zwischen einem Praxisadministrator und einem regulären Tierarzt unterschieden wird. Eine solche Hierarchie würde es ermöglichen, zwischen den Verwaltungsrechten eines Praxisinhabers und denen eines angestellten Tierarztes zu unterscheiden.

Darüber hinaus fehlen in der Entität \textit{veterinarians} fachspezifische Informationen wie Fachgebiet oder Qualifikationen. Da \textit{veterinarians} zwar über \textit{persons} grundlegende Personendaten wie Name und Kontaktinformationen erbt, fehlen jedoch tiermedizinisch relevante Attribute, was die Darstellung und Filterung von Tierärzten nach Fachgebiet in der Anwendung einschränkt.

Außerdem fehlt in der Tabelle \textit{appointments} ein Statusattribut, das den aktuellen Zustand eines Termins abbildet, wie etwa reserviert, ausstehend oder storniert. Ein solches Attribut wäre für den Buchungsprozess essenziell, da ohne eine klare Zustandsverwaltung keine eindeutige Unterscheidung zwischen freien, gebuchten und stornierten Terminen möglich ist.

Schließlich fehlt im Datenbankschema eine Tabelle für die Öffnungszeiten der Tierarztpraxen. Für ein Terminbuchungssystem ist dies eine wesentliche Lücke, da ohne diese Information keine automatische Validierung stattfinden kann, ob ein gebuchter Termin innerhalb der Öffnungszeiten einer Praxis liegt. Diese Funktionalität müsste daher vollständig in der Anwendungslogik des 
Backends abgebildet werden, was die Implementierungskomplexität erhöht.

Da die Korrektur dieser Schwachstellen den Umfang dieser Arbeit übersteigt, werden sie lediglich identifiziert und als Empfehlungen für eine zukünftige Weiterentwicklung festgehalten.

\section{Komponentendiagramm des Frontends}
Das Komponentendiagramm in Abbildung~\ref{fig:komponentendiagramm} zeigt den strukturellen Aufbau des Frontends von \textit{petappoint}. Ein Komponentendiagramm zeigt die Organisation und Abhängigkeiten von Softwarekomponenten innerhalb eines Systems \cite[S.~19]{sparxsystems2026}. 
Komponenten sind dabei austauschbare Teile eines Systems, die eine bestimmte Funktionalität erfüllen und ihre Dienste über definierte Schnittstellen anbieten. Die Architektur gliedert sich in drei Schichten, die dem in Abschnitt~\ref{sec:architektur} 
beschriebenen Schichtenmodell entsprechen. Durch diese klare Trennung wird die Austauschbarkeit einzelner Schichten gewährleistet, solange die definierten Schnittstellen erhalten bleiben~\cite[S.~168]{sommerville2018}.

Die \textbf{Präsentationsschicht} umfasst alle Screens der Anwendung und ist ausschließlich für die Darstellung der Benutzeroberfläche und die Nutzerinteraktion zuständig. Sie enthält keinerlei Geschäftslogik, sondern delegiert alle Operationen an die darunterliegende Business-Schicht. Die Screens sind in zwei Kategorien unterteilt: Tab-Screens, die über eine persistente Tab-Navigation erreichbar sind, und Modal-Screens, die kontextabhängig als überlagernde Ansichten geöffnet werden. Die konkreten Screens, deren Aufbau sowie das Navigationskonzept werden in den 
nachfolgenden Abschnitten detailliert beschrieben.

Die \textbf{Business-Schicht} kapselt die gesamte Geschäftslogik der Anwendung in Form von Funktionen, die in der Abbildung als Custom Hooks bezeichnet werden. Custom Hooks sind wiederverwendbaren Funktionen, die Geschäftslogik kapseln und von mehreren Komponenten genutzt werden können~\cite{react-custom-hooks}. Diese sind thematisch gruppiert und stellen den Screens die benötigten Daten und Operationen zur Verfügung. Durch die Kapselung der Logik in Funktionen wird sichergestellt, dass dieselbe Logik von mehreren Screens wiederverwendet werden kann, ohne dass Code dupliziert werden muss. Die Custom Hooks kommunizieren ausschließlich mit der Datenzugriffsschicht und haben keinen direkten Zugriff auf das Backend.

Die \textbf{Datenzugriffsschicht} besteht aus spezialisierten API-Modulen, die jeweils für einen bestimmten Fachbereich zuständig sind, wie etwa Authentifizierung, Terminverwaltung oder Tierarztpraxen. Alle Module kommunizieren ausschließlich über einen zentralen API-Client, der als einziger Kommunikationspunkt zum Backend fungiert. Der API-Client übernimmt dabei die Aufgabe eines HTTPS-Adapters, der Anfragen über die 
REST-API des Backends weiterleitet und die Antworten an die jeweiligen API-Module zurückgibt. Durch diese zentrale Stelle lassen sich übergreifende Aspekte wie Fehlerbehandlung einheitlich verwalten.

\clearpage
\begin{figure}[H]
    \centering
    \includesvg[width=1.0\linewidth]{figures/component-diagram.svg}
    \caption{Komponentendiagramm des Frontends}
    \label{fig:komponentendiagramm}
\end{figure}

\section{Navigationskonzeptdiagramme des Frontends}

Das Navigationskonzept beschreibt die Struktur und den Ablauf der Navigation innerhalb von \textit{petappoint} und bildet die Grundlage für die spätere Implementierung der Benutzeroberfläche. Die Navigation gliedert sich in drei hierarchische Bereiche: den Stack der für die Authentifizierung (Authentication Stack) zuständig ist, die Tab Navigation sowie die Modal Stacks. Da \textit{petappoint} zwei Nutzerrollen unterstützt (Tierhalter und Tierarztpraxis), unterscheiden sich Tab Navigation und Modal Stacks rollenabhängig voneinander, während der Authentication Stack für beide Rollen identisch ist. Die Navigationsdiagramme sind in Abbildung~\ref{fig:nav-tierhalter} sowie Abbildung~\ref{fig:nav-tierarzt} dargestellt.

\subsection{Authentication Stack}

Beim Start der Anwendung wird geprüft, ob Nutzende bereits
authentifiziert sind. Ist dies nicht der Fall, werden sie in den Authentication Stack geleitet, der den vollständigen Authentifizierungsfluss abbildet. Von der zentralen Authentifizierungskomponente aus stehen zwei Pfade zur Verfügung: Login und Registrierung. Über den Login-Screen können Nutzende bei vergessenem Passwort einen Zurücksetzungsprozess anstoßen, nach dessen Abschluss sie wieder zum Login zurückgeführt werden. Nach erfolgreichem Login werden Nutzende unmittelbar zur Tab Navigation weitergeleitet. Die Registrierung umfasst hingegen zwei zusätzliche Verifikationsschritte: die Bestätigung der E-Mail-Adresse sowie die Verifizierung der Telefonnummer. Erst nach erfolgreichem Durchlaufen beider Schritte erhalten Nutzende Zugang zur Tab Navigation.

\subsection{Tab Navigation und Modal Stacks}

Nach erfolgreicher Authentifizierung bildet die Tab Navigation den 
zentralen Einstiegspunkt der Anwendung. Sie ist dauerhaft am unteren Bildschirmrand sichtbar und ermöglicht den direkten Wechsel zwischen den Hauptbereichen. Welche Tab-Screens und Modal Stacks bereitgestellt werden, ist abhängig von der Nutzerrolle.

Für die Rolle \textbf{Tierhalter} stehen vier Tab-Screens zur Verfügung: \textit{Home}, \textit{Haustiere}, \textit{Termine} und \textit{Profil}. Der Home-Screen bildet den zentralen Ausgangspunkt der Anwendung. Der Haustiere-Screen dient der Verwaltung der eigenen Tiere, der Termine-Screen gibt einen Überblick über bestehende Buchungen und der Profil-Screen ermöglicht die Verwaltung der Nutzerdaten. Die Modal Stacks dieser Rolle umfassen mehrere kontextabhängige Abläufe. Der zentrale Buchungsfluss ist vom Home- sowie vom Termine-Screen aus zugänglich und 
führt Nutzende schrittweise über die Terminsuche, die Suchergebnisse und die Praxisdetailansicht bis zur Buchungsübersicht und abschließenden Buchungsbestätigung, nach der Nutzende zur Tab Navigation zurückgeleitet werden. Weitere Modal Stacks ermöglichen das Hinzufügen und Bearbeiten von Haustieren, das Bearbeiten und Stornieren von Terminen sowie die Anpassung des Nutzerprofils und die Einsicht in gespeicherte Praxisfavoriten.

Für die Rolle \textbf{Tierarztpraxis} stehen zwei Tab-Screens zur 
Verfügung: \textit{Buchungsübersicht} und \textit{Terminslots}. Die 
Buchungsübersicht listet alle eingehenden Buchungen und ermöglicht 
das Einsehen der jeweiligen Buchungsdetails. Der Terminslots-Screen 
dient der Verwaltung des eigenen Terminangebots, wobei neue Slots 
angelegt sowie bestehende Termine bearbeitet oder storniert werden 
können.

\begin{figure}[H]
    \centering
    \includesvg[width=1.0\linewidth]{figures/petowner-navigation-diagram.svg}
    \caption{Navigationsdiagramm der Tierhalter-Rolle}
    \label{fig:nav-tierhalter}
\end{figure}

\begin{figure}[H]
    \centering
    \includesvg[width=1.0\linewidth]{figures/vetpractice-navigation-diagram.svg}
    \caption{Navigationsdiagramm der Tierarztpraxis-Rolle}
    \label{fig:nav-tierarzt}
\end{figure}

\section{Mockups zur Darstellung des User Interface}

Zur Veranschaulichung der geplanten Benutzeroberfläche wurden Mockups 
für alle Screens von \textit{petappoint} erstellt. Mockups dienen als visuelle Grundlage \cite[S.~112]{pohl2021} für die spätere Implementierung und zeigen den strukturellen Aufbau sowie die Anordnung der UI-Elemente (engl. \textit{User Interface Elements}), also der visuellen und interaktiven Komponenten einer digitalen
Benutzeroberfläche~\cite[S.~4]{galitz2002}. Die Mockups sind entsprechend der Navigationsstruktur in vier Gruppen unterteilt: Authentifizierung, Tab-Ansichten der Tierhalter, Buchungsprozess und Tab-Ansichten der Tierarztpraxis. Für die Erstellung der Mockups wurde das KI-gestützte Prototyping-Tool \textit{v0} von Vercel eingesetzt \cite{v0-vercel}, das auf Basis textueller Beschreibungen detaillierte UI-Entwürfe generiert und eine schnelle visuelle Ausarbeitung der geplanten Screens ermöglicht. Die generierten Entwürfe wurden anschließend manuell geprüft und an die definierten Anforderungen angepasst.

\subsection{Mockups zur Authentifizierung}

Abbildung~\ref{fig:mockup-auth} zeigt die Screens des Authentication Stacks, der für beide Nutzerrollen identisch ist. Der Einloggen-Screen 
enthält Eingabefelder für E-Mail-Adresse und Passwort sowie einen Link zum Zurücksetzen des Passworts. Über diesen Link gelangen Nutzende zum Passwort-zurücksetzen-Screen, auf dem eine E-Mail-Adresse eingegeben werden kann, um einen Zurücksetzungslink zu erhalten. Der Konto-erstellen-Screen erfasst alle für die Registrierung erforderlichen Nutzerdaten. Abhängig vom gewählten
Registrierungspfad folgt anschließend entweder der
E-Mail-Verifizierungs- oder der Telefonnummer-Verifizierungsscreen, auf dem ein zugesandter Code eingegeben werden muss, um die
Identität zu bestätigen.

\clearpage
\begin{figure}[H]
    \centering
    \includesvg[width=1.0\linewidth]{figures/mockups-authentication.svg}
    \caption{Mockups für die Authentifizierung}
    \label{fig:mockup-auth}
\end{figure}
\clearpage
\subsection{Tab-Ansichten der Tierhalter}

Abbildung~\ref{fig:mockup-tabs-tierhalter} zeigt die vier Tab-Screens der Tierhalter-Rolle. Ein persistenter Suchbutton zur gezielten Suche nach einer Tierarztpraxis ist auf allen Screens verfügbar und ermöglicht den schnellen Zugang zum Buchungsfluss. Am unteren Bildschirmrand ist 
durchgehend die Tab-Leiste sichtbar.

Der \textit{Home}-Screen zeigt Nutzenden ihre wichtigsten persönlichen Informationen, darunter Name, Adresse und Profilbild. Darunter wird eine kompakte Vorschau des nächsten anstehenden Termins angezeigt. Den unteren Bereich des Screens nehmen nahegelegene Tierarztpraxen ein.

Der \textit{Termine}-Screen unterteilt sich in zwei Bereiche: Im Bereich der bevorstehenden Termine werden die wichtigsten Informationen je Termin in einer Karte dargestellt, von der aus der Termin bearbeitet oder storniert werden kann. Ein zweiter Bereich listet vergangene Termine auf, die direkt erneut gebucht werden können.

Der \textit{Haustiere}-Screen zeigt eine Übersicht aller angelegten Tiere mit den jeweiligen Kerninformationen sowie einem Tierbild. Über dedizierte Buttons kann jedes Tier bearbeitet oder ein neues Tier hinzugefügt werden.

Der Profil-Screen zeigt Nutzenden zunächst eine Kurzübersicht mit der Anzahl ihrer Termine und Haustiere. Darunter befindet sich eine Liste mit Einstellungsbereichen.

\begin{figure}[H]
    \centering
    \includesvg[width=1.0\linewidth]{figures/mockups-tabs-petowner.svg}
    \caption{Mockups der Tab-Ansichten des Tierhalters}
    \label{fig:mockup-tabs-tierhalter}
\end{figure}

\subsection{Buchungsprozess}

Abbildung~\ref{fig:mockup-buchung} zeigt den modalen Buchungsfluss der Tierhalter-Rolle. Der Buchungsprozess wird über den persistenten 
Suchbutton gestartet, woraufhin sich ein Modal öffnet, in dem Nutzende den gewünschten Ort, das betreffende Tier sowie die gewünschte
Behandlung eingeben können.

Die Suchergebnisse werden anschließend als modaler Screen dargestellt, der die gefundenen Tierarztpraxen in einer Liste aufführt. Je Eintrag 
werden die wichtigsten Informationen wie Name, Ort und Entfernung angezeigt. Bei Auswahl einer Praxis öffnet sich die Praxisdetailansicht mit weiterführenden Informationen: Öffnungszeiten, 
angebotene Leistungen, verfügbare Tierärzte sowie ein Kalender zur Auswahl eines freien Termins.

Nach der Auswahl von Datum und Uhrzeit wird Nutzenden eine Buchungsübersicht präsentiert, die die gewählte Praxis, das betreffende Tier sowie die gewünschte Behandlung zusammenfasst. Tier und Behandlung können an dieser Stelle noch nachträglich angepasst werden. Nach dem Bestätigen der Buchung erscheint abschließend der Buchungsbestätigungsscreen mit einer Zusammenfassung aller relevanten Termininformationen.

\begin{figure}[H]
    \centering
    \includesvg[width=1.0\linewidth]{figures/mockups-booking-process.svg}
    \caption{Mockups des Buchungsprozesses}
    \label{fig:mockup-buchung}
\end{figure}
\clearpage

\subsection{Tab-Ansichten der Tierarztpraxis}

Abbildung~\ref{fig:mockup-tierarzt} zeigt die Screens der Tierarztpraxis-Rolle. Die Tab Navigation umfasst zwei Bereiche: 
\textit{Buchungen} und \textit{Terminslots}.

Der \textit{Buchungen}-Screen listet alle eingegangenen Buchungen der Praxis auf. Die Liste lässt sich nach Status filtern (bestätigt, ausstehend oder storniert), um einen gezielten Überblick zu ermöglichen. Bei Auswahl einer Buchung öffnet sich ein modaler Screen mit allen relevanten Informationen zum jeweiligen 
Termin. Über einen Button am unteren Rand dieses Screens kann der Termin bei Bedarf storniert werden.

Der \textit{Terminslots}-Screen zeigt die Terminslots des aktuellen Tages in einer tagesbasierten Ansicht, die sich durch Navigation 
vor- und zurückblättern lässt. Für jeden Slot stehen die Aktionen Bearbeiten und Löschen zur Verfügung. Zudem können neue Slots hinzugefügt werden. Jede dieser Aktionen öffnet einen eigenen 
modalen Screen, über den die jeweilige Operation durchgeführt wird.

\begin{figure}[H]
    \centering
    \includesvg[width=1.0\linewidth]{figures/mockups-vetpractices.svg}
    \caption{Mockups der Tierarztpraxis}
    \label{fig:mockup-tierarzt}
\end{figure}
\clearpage

\section{Aktivitätsdiagramm der Terminbuchung}

Das Aktivitätsdiagramm in Abbildung~\ref{fig:aktivitaet-buchung-1} und Abbildung~\ref{fig:aktivitaet-buchung-2} 
beschreibt den vollständigen Ablauf des Terminbuchungsprozesses aus Sicht des Tierhalters. Der Prozess beginnt mit dem Öffnen des Suchscreens, auf dem Nutzende zwischen drei Suchmöglichkeiten
wählen können: der Suche nach Standort, nach einer konkreten Tierarztpraxis oder nach einem Tierarzt beziehungsweise einer Behandlung. Auf Basis der eingegebenen Suchkriterien werden die entsprechenden Tierarztpraxen als Ergebnisliste angezeigt, aus der eine Praxis ausgewählt wird. Optional kann die Praxis an dieser Stelle favorisiert werden.

Im nächsten Schritt wählen Nutzende einen verfügbaren Termin aus dem Kalender der Praxis aus. Anschließend wird geprüft, ob die Suche nach einer Behandlung gefiltert wurde. Wurde nur eine
Behandlung gefiltert, wird diese direkt übernommen. Wurden mehrere Behandlungen gefiltert oder kein Filter gesetzt, wählen Nutzende die gewünschte Behandlung manuell aus.

Daraufhin wird geprüft, ob bereits ein Tier angelegt wurde. Ist dies nicht der Fall, werden Nutzende zunächst aufgefordert, ein Tier anzulegen, bevor sie fortfahren können. Andernfalls wählen sie direkt eines ihrer vorhandenen Tiere aus.
Nach der Tierauswahl wird Nutzenden eine Terminzusammenfassung präsentiert, die die gewählte Praxis, den Termin, die Behandlung sowie das ausgewählte Tier aufführt. Nutzende haben an dieser
Stelle die Möglichkeit, Behandlung, Termin oder Tier nachträglich zu bearbeiten, woraufhin der jeweilige Auswahlschritt erneut durchlaufen wird. Nach Bestätigung der Zusammenfassung wird
die Buchung abgeschlossen und Nutzende werden zur Terminübersicht weitergeleitet. Der Prozess kann zu jedem Zeitpunkt abgebrochen werden, wodurch der Buchungsvorgang beendet und Nutzende ebenfalls zur Terminübersicht zurückgeführt werden.


\begin{landscape}
\begin{figure}[H]
    \centering
    \includesvg[inkscapelatex=false, width=1.0\linewidth]{figures/activity-diagram1.svg}
    \caption{Aktivitätsdiagramm des Terminbuchungsprozesses (Teil 1)}
    \label{fig:aktivitaet-buchung-1}
\end{figure}
\end{landscape}
\clearpage
\begin{landscape}
\begin{figure}[H]
    \centering
    \includesvg[inkscapelatex=false, width=1.0\linewidth]{figures/activity-diagram2.svg}
    \caption{Aktivitätsdiagramm des Terminbuchungsprozesses (Teil 2)}
    \label{fig:aktivitaet-buchung-2}
\end{figure}
\end{landscape}